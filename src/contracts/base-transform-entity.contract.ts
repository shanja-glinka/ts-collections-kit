/**
 * Контракт для трансформации plain объектов в экземпляры сущностей.
 * Можно расширять данный контракт при необходимости.
 */
export interface IBaseTransformEntityContract {
  // Пока что не определяем методы, но в дальнейшем здесь можно задать обязательные методы для трансформации.
}
